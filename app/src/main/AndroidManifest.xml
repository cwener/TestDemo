<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.test">

    <!-- 在这里添加权限 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".activity.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Test">
        <activity
            android:name=".activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.LayerTestActivity" />
        <activity android:name=".activity.MemoryActivity" />
        <activity android:name=".activity.VolumeTestActivity" />
        <activity android:name=".activity.acoptionstest.ActivityA" />
        <activity android:name=".activity.acoptionstest.ActivityB" />
        <activity android:name=".activity.FontTestActivity" />
        <activity
            android:name=".activity.acoptionstest.ActivityC"
            android:theme="@style/AppTheme.TranslucentTheme" />
        <activity android:name=".activity.land.LandActivity" android:screenOrientation="sensorLandscape"/>
        <activity android:name=".activity.land.LandActivity2" android:screenOrientation="sensorLandscape"/>
        <activity android:name=".activity.land.LandActivity3" android:screenOrientation="sensorLandscape"/>
    </application>

</manifest>